<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Games Attending

    <a data-toggle="collapse" href="#user_games_attending_new_form">
      Add attendee
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @user.games_attending.each do |attendee| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <%= link_to attendee, attendee %>

        <%= link_to attendee, method: :delete, data: { confirm: "Do you really want to delete this attendee" } do %>
          <i class="fas fa-trash-alt"></i>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="user_games_attending_new_form">
        <%= render partial: "users/user_games_attending_new_form" %>
      </div>
    </div>

  </ul>
</div>
